#################################
#             Shadows           #
#################################

shadow-radius = 7;
shadow-opacity = .75
shadow-offset-x = -7;
shadow-offset-y = -7;

shadow-exclude = [
  "name = 'Notification'",
  "class_g ?= 'Notify-osd'",
  "_GTK_FRAME_EXTENTS@:c"
];

#################################
#           Fading              #
#################################

fading = true;
fade-in-step = 0.028;
fade-out-step = 0.028;
fade-delta = 8;

#################################
#          Animations           #
#################################

animations = true;
animation-stiffness = 120;
animation-window-mass = 0.8;
animation-dampening = 25;
animation-clamping = false;

animation-for-open-window = "zoom";
animation-for-unmap-window = "slide-down";
animation-for-prev-tag = "slide-out-center";
animation-for-next-tag = "slide-in-center";
animation-for-workspace-change-in = "slide-left";
animation-for-workspace-change-out = "slide-right";

#################################
#   Transparency / Opacity      #
#################################

inactive-opacity = 0.8;
frame-opacity = 1.0;
inactive-opacity-override = false;

# Make popups, dialogs, and menus fully opaque
opacity-rule = [ 
  "94:class_g = 'Geany'",
  "100:window_type = 'dialog'",
  "100:window_type = 'popup_menu'",
  "100:window_type = 'dropdown_menu'",
  "100:window_type = 'menu'",
  "100:window_type = 'utility'",
  "100:window_type = 'notification'",
  "100:window_type = 'splash'",
  "100:window_type = 'toolbar'",
  "100:_NET_WM_STATE@:32a *= '_NET_WM_STATE_MODAL'"
];

#################################
#           Corners             #
#################################

corner-radius = 8;

rounded-corners-exclude = [
  "window_type = 'dock'",
  "window_type = 'desktop'",
  "window_type = 'menu'",
  "window_type = 'dropdown_menu'",
  "window_type = 'popup_menu'",
  "window_type = 'tooltip'"
];

#################################
#     Background-Blurring       #
#################################

blur-kern = "3x3box";

blur-background-exclude = [
  "window_type = 'dock'",
  "window_type = 'desktop'",
  "_GTK_FRAME_EXTENTS@:c"
];

#################################
#       General Settings        #
#################################

backend = "glx";

dithered-present = false;
vsync = true;

mark-wmwin-focused = true;
mark-ovredir-focused = true;
detect-rounded-corners = true;
detect-client-opacity = true;
detect-transient = true;
use-damage = true;

# GLX backend settings
glx-no-stencil = true;
glx-no-rebind-pixmap = true;

log-level = "warn";

wintypes:
{
  tooltip = { fade = true; shadow = true; opacity = 1.0; focus = true; full-shadow = false; };
  dock = { shadow = false; clip-shadow-above = true; }
  dnd = { shadow = false; }
  popup_menu = { opacity = 1.0; shadow = true; }
  dropdown_menu = { opacity = 1.0; shadow = true; }
  menu = { opacity = 1.0; shadow = true; }
  utility = { opacity = 1.0; shadow = true; }
  dialog = { opacity = 1.0; shadow = true; }
  notification = { opacity = 1.0; shadow = true; }
};
